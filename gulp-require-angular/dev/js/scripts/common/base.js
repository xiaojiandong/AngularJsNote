
define(['jquery','jquery_confirm','angular','bootstrap','router'],
    function(jquery,jquery_confirm,angular,bootstrap,router){

    return SUBJ = {
       angularInit : function (app) {
           //angular.bootstrap(document,[app]);
           //console.log(app);
       }
    };

});